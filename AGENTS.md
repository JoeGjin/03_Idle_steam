# Idle Steam 项目协作规则

## 项目阶段与目标

- 本项目是 Godot 4.6 / GDScript 开发的 Windows 桌面挂件与放置游戏。
- 项目已完成原型，目前处于 pre-production，当前目标是产出 vertical slice。
- 现阶段功能优先级依次为：世界生成、记忆系统、存档、UI，以及后续系统。
- 优先完成可验证的纵向体验，不主动进行与当前目标无关的大规模重构。

## 沟通语言

- 默认使用中文回复。
- 代码注释和文档默认使用中文；Godot API、类名、信号名及其他代码标识符保持英文。
- 注释数量由实现需要决定，重点解释设计意图、非显然约束和容易误用的逻辑，避免逐行复述代码。
- 遇到会改变功能方向、数据结构、场景结构或工作流的选择时，先说明取舍并询问用户。

## 修改授权

- 可以修改项目内的 `.gd`、`.tscn`、`.tres`、`project.godot` 和其他文件，但任何修改开始前都必须先说明拟修改的文件、目标和影响，并取得用户许可。
- 用户对某项具体修改的明确授权仅适用于该项修改；不要顺带扩大修改范围。
- 未经许可只进行读取、搜索、分析和建议，不写入文件。
- 修改前检查工作区状态，保留用户已有的未提交改动。不要覆盖、还原或清理与当前任务无关的更改。
- 不执行破坏性 Git 操作，不擅自提交、推送、切换分支或重写历史。

## Godot 场景与资源

- 原则上通过 Godot 编辑器修改 `.tscn` 和 `.tres`。
- 需要修改场景或资源时，优先向用户说明要在 Godot 编辑器中调整的节点、属性和资源字段，由用户决定是否自行操作。
- 如果直接编辑文本形式的 `.tscn` 或 `.tres` 更合适，可以说明修改的可能性、风险和范围，再询问用户是否授权直接编辑。
- 不手动修改 `.godot/`、`.import` 或其他 Godot 生成缓存。
- 保留已有资源 UID、节点路径和外部资源引用；未经确认不要批量重排场景或重写资源文件。

## GDScript 实现原则

- 静态类型不是强制要求。类型能明显提高可读性、编辑器提示或减少错误时使用；简单局部逻辑可以依赖类型推断。
- 根据耦合程度和所有权选择信号或直接节点调用，不机械强制某一种模式。
- 跨模块事件、需要多个监听者或需要解耦时优先考虑信号；明确的一对一协作和有清晰所有权的调用可以直接调用节点或对象。
- 保持修改小而聚焦，遵循附近代码的命名、缩进和组织方式。
- 不为了“统一风格”改动无关代码，也不在功能修改中夹带大范围格式化。
- 新增或重命名类、节点、信号、资源字段时，搜索并检查所有引用方。

## 项目结构与术语

- 主场景为 `scenes/Main.tscn`，主要协调脚本为 `scripts/main/Main.gd`。
- `scripts/main/controllers/` 存放输入、角色状态、动画、音频、世界和记忆等控制逻辑。
- `scripts/main/world/` 存放世界与宠物表现逻辑；`scripts/main/ui/` 存放 UI；`scripts/main/output/` 存放最终窗口输出逻辑。
- 世界定义位于 `scripts/main/controllers/world/defs/`，相关工作流和图层约束参考根目录 `说明.txt`。
- 状态定义位于 `scripts/main/controllers/status/defs/`。
- 记忆定义位于 `scripts/main/controllers/memory/defs/`，资源类型为 `MemoryDef`。
- 记忆系统术语统一使用 `Memory`。当前正在从旧的 `Sticker` 命名迁移；遇到残留 `StickerController`、`sticker_*` 等名称时，将其视为待迁移内容，不要据此建立新的旧命名 API。重命名前先说明影响范围并取得许可。

## Windows GDExtension

- `addons/global_key_hook/` 是用于后台键盘输入的 Windows C++ GDExtension。
- 禁止直接修改其中的 C++、构建脚本和二进制文件。
- 如果任务确实需要修改该扩展，只分析问题并向用户提出具体建议、风险和预计涉及的文件，等待用户另行决定。
- 不提交或处理 `~global_key_hook...`、临时 DLL/PDB、对象文件等构建产物，除非用户明确要求。

## 验证与交付

- 当前没有强制自动化测试或固定验证流程，不要把新增测试或运行完整验证作为每次修改的默认前置条件。
- 根据改动风险建议合适的验证方式，例如 Godot 编辑器加载、运行主场景或针对性的人工交互检查；需要启动或执行验证前说明用途。
- 涉及世界生成时，重点检查世界切换、图层生成和资源引用。
- 涉及记忆系统时，重点检查 `Memory` 命名一致性、资源字段、标签、池分类和所有调用方。
- 交付时用中文简要说明修改了什么、影响哪些文件、尚未验证什么，以及用户需要在 Godot 编辑器中完成的步骤。
